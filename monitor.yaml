apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: devops-monitor
  labels:
    release: monitoring
spec:
  selector:
    matchLabels:
      app: devops-project
  endpoints:
    - port: http
      path: /metrics
      interval: 15s