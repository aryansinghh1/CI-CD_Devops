apiVersion: v1
kind: Service
metadata:
  name: devops-project-service
  labels:
    app: devops-project
spec:
  type: NodePort
  selector:
    app: devops-project
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 3000
      nodePort: 30007